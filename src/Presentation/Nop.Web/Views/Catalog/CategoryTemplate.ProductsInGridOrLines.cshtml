@model CategoryModel
@{
    Layout = "~/Views/Shared/_ColumnsTwo.cshtml";
    string ManufacturerName = ViewBag.ManufacturerName;
    if (!String.IsNullOrEmpty(ViewBag.ManufacturerSEName))
    {
        //Html.AddTitleParts("Jual " + Model.SeName + " " + ViewBag.ManufacturerName + " harga murah, Spesifikasi dan Garansi Resmi");
        Html.AddTitleParts("Jual " + Model.Name + " " + ManufacturerName + " beli dengan harga murah , Spesifikasi dari Eldago");
        Html.AddMetaDescriptionParts("Cari , Jual dan beli " + Model.Name + " " + ManufacturerName + " beli dengan harga murah, Spesifikasi dari Eldago sebagai Pusat Belanja khusus Bisnis dan distributor, supplier dari " + Model.Name + " " + ManufacturerName);
        Html.AddMetaKeywordParts(Model.MetaKeywords);
    }
    else
    {
        Html.AddTitleParts(!String.IsNullOrEmpty(Model.MetaTitle) ? Model.MetaTitle : Model.Name);
        Html.AddMetaDescriptionParts(Model.MetaDescription);
        Html.AddMetaKeywordParts(Model.MetaKeywords);
    }



    var canonicalUrlsEnabled = EngineContext.Current.Resolve<SeoSettings>().CanonicalUrlsEnabled;
    if (canonicalUrlsEnabled)
    {
        if (!String.IsNullOrEmpty(ViewBag.ManufacturerSEName))
        {
            var categoryUrl = Url.RouteUrl("Category", new { SeName = "merek/" + Model.SeName + "/" + ViewBag.ManufacturerSEName }, this.Request.Url.Scheme);
            Html.AddCanonicalUrlParts(categoryUrl);
        }
        else
        {
            var categoryUrl = Url.RouteUrl("Category", new { SeName = Model.SeName + "/" }, this.Request.Url.Scheme);
            Html.AddCanonicalUrlParts(categoryUrl);
        }
    }

    var breadcrumbDelimiter = EngineContext.Current.Resolve<CommonSettings>().BreadcrumbDelimiter;
}
@using Nop.Core.Domain.Common;
@using Nop.Core.Domain.Seo;
@using Nop.Core.Infrastructure;
@using Nop.Web.Models.Catalog;
@*category breadcrumb*@
@section Breadcrumb
{
    @if (Model.DisplayCategoryBreadcrumb)
    {
        <div class="breadcrumb">
            <ul>
                <li>
                    <a href="@Url.RouteUrl("HomePage")" title="@T("Categories.Breadcrumb.Top")">@T("Categories.Breadcrumb.Top")</a>
                    <span class="delimiter"><i class="fa fa-angle-right" aria-hidden="true"></i></span>
                </li>
                @foreach (var cat in Model.CategoryBreadcrumb)
                {
                    var isLastCategory = cat.Id == Model.Id;
                    <li>
                        @if (isLastCategory)
                        {
                            <strong class="current-item">@cat.Name</strong>
                        }
                        else
                        {
                            <a href="@Url.RouteUrl("Category", new { SeName = cat.SeName })" title="@cat.Name">@cat.Name</a>
                            <span class="delimiter"><i class="fa fa-angle-right" aria-hidden="true"></i></span>
                        }
                    </li>
                }
            </ul>
        </div>
        @Html.Widget("categorydetails_after_breadcrumb", Model.Id)
    }
}
<div class="page category-page">
    <div class="page-title">
        <h1>@Model.Name</h1>
    </div>
    <div class="page-body">



        @Html.Widget("categorydetails_before_subcategories", Model.Id)
        @*subcategories*@
        @if (Model.SubCategories.Count > 0)
        {
            <div class="category-grid sub-category-grid heightshow clearfix">
                <div class="item-grid">
                    <div class="text-center">
                        @foreach (var item in Model.SubCategories)
                        {
                            <div class="width-cat">
                                <div class="item-box-product">
                                    <div class="sub-category-item">
                                        <h2 class="title">
                                            <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                                @item.Name
                                            </a>
                                        </h2>
                                        <div class="picture fix-width box-cat">
                                            <a href="@Url.RouteUrl("Category", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                                <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
            <span class="col-sm-12 showmorecat">Show More Category</span>
            <div class="clearfix"></div>
        }
        @Html.Widget("categorydetails_before_featured_products", Model.Id)
        @*featured products*@
        @*@if (Model.FeaturedProducts.Count > 0)
            {
                <div class="product-grid featured-product-grid">
                    <div class="title">
                        <strong>@T("Products.FeaturedProducts")</strong>
                    </div>
                    <div class="item-grid clearfix">
                        <div class="row">
                            @foreach (var item in Model.FeaturedProducts)
                            {
                                <div class="col-lg-3 col-sm-4 col-xs-6">
                                    <div class="item-box">
                                        @Html.Partial("_ProductBox", item)
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }*@


        @Html.Widget("categorydetails_after_featured_products", Model.Id)
        @if (Model.Products.Count > 0)
        {
            @Html.Partial("_CatalogSelectors", Model.PagingFilteringContext, new ViewDataDictionary())
        }
        @section left {
            @Html.Widget("categorydetails_before_filters", Model.Id)
            <div class="product-filters clearfix">
                @*filtering*@
                @if (Model.PagingFilteringContext.PriceRangeFilter.Enabled)
                {
                    @Html.Partial("_FilterPriceBox", Model.PagingFilteringContext.PriceRangeFilter, new ViewDataDictionary())
                }
                @*filtering*@
                @if (Model.PagingFilteringContext.SpecificationFilter.Enabled)
                {
                    @Html.Partial("_FilterSpecsBox", Model.PagingFilteringContext.SpecificationFilter, new ViewDataDictionary())
                }

            </div>
            <script type="text/javascript">
                jQuery(function ($) {
                    $(".iner-filter .block-title").click(function () {
                        $(this).parent().children('.block-content').slideToggle(300);
                        $(this).toggleClass('tutup')
                    });
                    $(".iner-filter .category-list li").before().click(function () {
                        if ($(this).hasClass("opened")) {
                            $(this).children("ul").slideUp();
                            $(this).removeClass("opened");
                        } else {
                            $(this).children("ul").slideDown();
                            $(this).addClass("opened");
                        }
                    });
                });
            </script>
        }
        @Html.Widget("categorydetails_before_product_list", Model.Id)

        @if (Model.Products.Count > 0)
        {
            <div class="@(Model.PagingFilteringContext.ViewMode == "list" ? "product-list" : "product-grid")">
                <div class="item-grid clearfix">
                    @foreach (var product in Model.Products)
                    {
                        <div class="width-col">
                            <div class="item-box">
                                @Html.Partial("_ProductBox", product)
                            </div>
                        </div>
                    }
                </div>
            </div>
        }
        @{
            var pager = Html.Pager(Model.PagingFilteringContext).QueryParam("pagenumber");
        }
        @if (!pager.IsEmpty())
        {
            <div class="pager">
                @pager
            </div>
        }

        @Html.Widget("categorydetails_bottom", Model.Id)
    </div>


    @*@Html.Widget("categorydetails_top", Model.Id)
        @*description
        @if (!String.IsNullOrWhiteSpace(Model.Description))
        {
            <div class="category-description">
                @Html.Raw(Model.Description)
            </div>
        }*@


    @Html.Widget("categorydetails_top", Model.Id)
    @*description*@
    @if (!String.IsNullOrWhiteSpace(Model.Description))
    {
        <div class="category-description">
            @*<h2 class="title-desc">
                    Description @Model.Name
                </h2>*@
            @Html.Raw(Model.Description)
        </div>
    }
</div>
